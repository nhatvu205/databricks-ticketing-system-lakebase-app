# Reflection draft

Customize this to match what you actually experienced, keeping it to three through five sentences:

> The most difficult part was connecting the Databricks App service principal to Lakebase with rotating OAuth credentials while keeping secrets out of the source. Lakebase differs from a traditional analytics table because it is PostgreSQL optimized for transactional inserts, updates, constraints, and low-latency application queries. Traditional analytics tables are instead optimized primarily for large scans, aggregations, and batch or streaming analytical workloads. Next, I would add ticket assignment, SLA deadlines, and notifications.
