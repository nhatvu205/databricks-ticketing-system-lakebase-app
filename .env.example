# Local-only optional settings. Never commit real secrets or passwords.
# For a local test PostgreSQL instance only, set DATABASE_URL outside Git.
# DATABASE_URL=<local-test-postgresql-connection-url>
# LOCAL_USER_EMAIL=your.name@example.com
